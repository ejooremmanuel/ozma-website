import React, { ReactNode } from "react";

type Props = {
  bgUrl: string;
  heading: ReactNode;
  subText: ReactNode;
};

const PageInformation = ({ bgUrl = "", heading, subText }: Props) => {
  return <></>;
};

export default PageInformation;
